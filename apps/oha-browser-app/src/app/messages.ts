import { defineMessage } from 'react-intl';

export const OPENING_HOURS_APP_MESSAGES = {
  sourceChoice: defineMessage({
    defaultMessage: 'Source choice:',
  }),
  chooseFile: defineMessage({
    defaultMessage: 'Choose file:',
  }),
  openingHours: defineMessage({
    defaultMessage: 'Opening hours:',
  }),
};
