import { defineMessage } from 'react-intl';

export const WOLT_UI_MESSAGES = {
  today: defineMessage({
    defaultMessage: 'Today',
    description: 'Today label',
  }),
  closed: defineMessage({
    defaultMessage: 'Closed',
    description: 'Closed label',
  }),
};
